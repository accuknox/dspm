# The image every instance runs. Change the tag here to update; the service pulls it before each run.
# Point it at a private mirror when the VM has no route to public.ecr.aws.
DSPM_IMAGE=public.ecr.aws/k9v9d5v2/dspm:v0.1.0-alpha.1
